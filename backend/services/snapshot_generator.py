"""Static HTML snapshot generator using Jinja2 — Phase 5 implementation.

Stub in Phase 1.
"""
from __future__ import annotations

from pathlib import Path


def generate_snapshot(db_session) -> Path:  # type: ignore[type-arg]
    """Render a self-contained HTML snapshot to exports/ and return its path.

    Phase 5 implementation pending.
    """
    raise NotImplementedError("HTML snapshot generation will be implemented in Phase 5.")
